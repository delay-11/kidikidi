/* =========================================================
 * 견적 요청 상태
========================================================= */
let quoteProd = "none";
let quoteDue = "";
let bizFileDataUrl = null;

/* =========================================================
 * 견적 추가 옵션 상태
========================================================= */
let keyringQty = 0;
let keyringLed = "none";
let keyringColor = "black";
let keyringSlots = "1";

let packType = "none";
let packSheet = false;
let packSticker = false;
let quoteNotes = "";

/* =========================================================
 * UI 잠금 상태
========================================================= */
let uiLocked = false;
